package boot;

import java.io.FileNotFoundException;
import presenter.Properties;
import presenter.PropertiesHandler;
import view.AbstractObservableView;
import view.BasicWindow;
import view.GameWindow;

public class Run2 {

	public static void main(String[] args) {
		
		Properties prop;
		try{
			prop = PropertiesHandler.getInstance();
			System.out.println("found");
		}catch (FileNotFoundException e) {
			prop = new Properties();
		}
		
			AbstractObservableView g = new GameWindow(prop);
		 //GameWindow g = new GameWindow(prop);
		 ((BasicWindow) g).run();
		 
		 
	
	}
	

}
